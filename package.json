{
  "name": "todolist_jwt_auth",
  "version": "1.0.0",
  "description": "dockerized full stack to do app using NodeJS backend, postgreSQL db, prisma OM, and JWT auth",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm ./src/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "express": "^5.1.0",
    "pg": "^8.14.1",
    "prisma": "^6.5.0"
  }
}
